# Requirements (from ingest)

## v1 (P0+P1)
Room create/end, QR/code join, Excalidraw teacher board, live sync, late-join bootstrap, student read-only, local pan/zoom, follow teacher, student count, anonymous doubts + rate limit, teacher queue, answer/dismiss, same-doubt vote, deterministic moderation + AI triage adapter, final board persist, image/PDF export, history page, reconnect UX, error/empty states, permission tests, 100-viewer load target.

All UI work follows the canvas-first, warm-neutral visual system in docs/05 and docs/06 plus `.planning/VISUAL_DIRECTION.md`; color fields are optional contained accents, never board overlays or status-only communication. Controls use Tactile UI System physics mapped onto Syncvas tokens.

## v2 (deferred)
Bookmarks, rewind/timeline, confusion pulse, AI structured notes, embedding clustering, teacher analytics, multi-writer, student annotations, HWR on live path, audio/video, attendance, paid plans, tenants.
