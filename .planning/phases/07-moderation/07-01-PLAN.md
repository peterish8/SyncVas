---
phase: 07-moderation
plan: 01
type: execute
wave: 1
depends_on: ["06-01"]
files_modified: ["convex/doubts.ts", "convex/moderation.ts", "lib/ai/moderation-adapter.ts", "components/doubts/*", "tests/moderation.test.ts"]
requirements: ["MOD-01", "MOD-03", "MOD-04"]
---

<objective>Improve doubt quality code-first: deterministic rules handle obvious noise, duplicate suggestions remain explainable, and the provider-neutral adapter safely falls back to uncertain until Phase 10 configures an external provider.</objective>

<tasks>
1. Build deterministic profanity/noise/length/repeat classifiers before any adapter call. Store reason codes and minimal operational metadata, not redundant raw doubt text, and keep rules configurable/testable in code.
2. Define a provider-neutral moderation interface with strict request/result schemas and a disabled implementation that returns `uncertain` with a safe reason. Implement duplicate suggestion using normalized text and bounded session-scoped candidates, not embeddings or an unindexed scan.
3. Surface neutral/amber screening feedback and teacher duplicate suggestions without silently hiding plausible doubts. Test accept/reject/uncertain flows, adapter failure fallback, and the absence of hardcoded provider SDK imports in feature code.
</tasks>

<verification>
- Obvious junk is stopped without calling the adapter.
- Disabled, timed-out, malformed, and uncertain adapter outcomes preserve plausible doubts for the teacher queue.
- Duplicate hints are advisory; teacher actions and participant privacy remain unchanged.
</verification>

<done>MOD-01, MOD-03, and MOD-04 pass without credentials or third-party API traffic.</done>
