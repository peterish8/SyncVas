---
phase: 06-p0-final-board-persistence
plan: 01
type: execute
wave: 1
depends_on: ["05-01"]
files_modified: ["convex/sessions.ts", "convex/board.ts", "convex/schema.ts", "socket-server/src/server.ts", "components/teacher/*", "tests/final-board.test.ts"]
requirements: ["EXPORT-01"]
---

<objective>Close the P0 gate by finalizing an ended class into a durable final-scene record that can be safely restored, without treating routine pointer traffic as database data.</objective>

<tasks>
1. Define the finalization state machine: owner-only `live → ending → ended`, revoke new join admission first, request/capture the current canonical scene/version once, store large scene payloads in Convex file storage, and persist only indexed snapshot metadata.
2. Implement internal-only finalization/persist functions and idempotency guards. Public end requests schedule internal work; no scheduled job calls a public mutation. Ensure an interrupted finalization can resume without duplicate final snapshots.
3. Add teacher confirmation/status UI and read-only final-board restoration path. Test ownership, race/retry handling, storage failure behavior, fresh-session rejection, and restore after refresh for teacher and student.
</tasks>

<verification>
- P0 acceptance run: one teacher and two students end a class, then reopen/refresh to the same final board.
- Tests prove raw pointer events never enter Convex and finalization cannot be called by students.
- The user-facing failure state says what failed and how to retry without losing the active scene.
</verification>

<done>EXPORT-01 and every P0 acceptance condition are ready for the mandatory one-teacher/two-student gate.</done>
