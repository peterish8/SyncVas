---
phase: 03-room-lifecycle
plan: 01
type: execute
wave: 1
depends_on: ["02-01"]
files_modified: ["convex/schema.ts", "convex/sessions.ts", "convex/participants.ts", "app/teacher/page.tsx", "app/join/page.tsx", "components/room/*", "socket-server/src/server.ts", "tests/room-lifecycle.test.ts"]
requirements: ["ROOM-01", "ROOM-02", "ROOM-03", "ROOM-04", "ROOM-05"]
---

<objective>Replace the proof room with durable, authorized classroom sessions: authenticated teachers create/end a room; students join anonymously by short code or QR; live presence is coarse and room-scoped.</objective>

<tasks>
1. Implement the auth abstraction and public Convex session/participant functions from docs/10. Validate every argument, generate join codes server-side, use documented indexes, enforce teacher ownership, upsert only pseudonymous participants, and reject joins to non-live sessions.
2. Add teacher start/end UI, six-character join form, QR deep link, room metadata, and live participant count. Use code-first error/empty states and accessible controls; never request a student name.
3. Replace proof socket admission with short-lived signed room tokens issued after Convex validation. Derive role/session from the token, join only `session:{sessionId}`, publish coarse presence, and add tests for ended rooms, cross-room attempts, forged role fields, and code/QR equivalence.
</tasks>

<verification>
- Convex mutation/query tests cover argument validation, ownership, live/ended state, and minimal join metadata.
- Browser flow proves teacher create → code/QR join → count → end → rejected rejoin.
- Socket tests prove role/session come from the signed token, not browser fields.
</verification>

<done>ROOM-01 through ROOM-05 work with durable state and anonymous student privacy.</done>
