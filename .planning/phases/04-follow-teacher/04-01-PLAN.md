---
phase: 04-follow-teacher
plan: 01
type: execute
wave: 1
depends_on: ["03-01"]
files_modified: ["components/board/*", "components/student/*", "shared/protocol/socket.ts", "socket-server/src/server.ts", "tests/follow-mode.test.ts"]
requirements: ["FOLLOW-01", "FOLLOW-02", "FOLLOW-03", "FOLLOW-04"]
---

<objective>Implement a lossy teacher viewport stream that students may follow, leave locally by panning/zooming, and re-enter without touching board content.</objective>

<tasks>
1. Have the authorized teacher coalesce viewport updates to roughly 10–15 per second and emit validated `teacher:viewport` packets; server forwards only the latest validated room-scoped state.
2. Add student follow state and accessible controls: Following, manual navigation exits locally, and Return to Teacher applies the most recent teacher viewport. Do not put navigation state in Convex or broadcast a student's camera.
3. Test packet loss, reconnect, stale viewport ordering, local exit, return, and a three-client case proving board state remains unchanged by every viewport path.
</tasks>

<verification>
- Follow state is textually visible, keyboard reachable, and does not rely on color alone.
- Socket tests cover teacher-only viewport emission and cross-room isolation.
- Two students independently follow/free-roam without altering each other or teacher state.
</verification>

<done>FOLLOW-01 through FOLLOW-04 pass without coupling navigation to scene mutation.</done>
