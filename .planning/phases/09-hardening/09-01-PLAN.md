---
phase: 09-hardening
plan: 01
type: execute
wave: 1
depends_on: ["08-01"]
files_modified: ["tests/security/*", "tests/load/*", "socket-server/src/*", "convex/*", ".github/workflows/*", "README.md"]
requirements: ["SEC-01", "SEC-02", "SEC-03", "SEC-04", "LOAD-01"]
---

<objective>Prove the complete code-first product respects roles, rooms, scale, deploy configuration, and the teacher’s actual pen hardware before enabling a live provider integration.</objective>

<tasks>
1. Create adversarial permission tests for forged roles, token/session mismatch, cross-room events, student board mutation, Convex ownership, exports/history access, and browser-secret leakage. Fix enforcement at the shared server/function boundary, not just the UI.
2. Add reproducible one-teacher/100-viewer load smoke tooling with bounded rooms, version/presence measurements, disconnect cleanup, and a results artifact. Use this evidence to decide whether a Socket.IO scale adapter is actually necessary; do not pre-add it.
3. Document production environment contract, CI checks, error codes, deployment runbook, and a manual supported-browser XP-Pen test. Verify a production-like deploy has no privileged/secrets in the public bundle.
</tasks>

<verification>
- Automated permission-boundary suite covers all SEC IDs.
- Load run records inputs, outcomes, latency/error observations, and known limits.
- Production checklist includes desktop XP-Pen writing, two student tabs, refresh, follow/free-roam, doubts, finalization, export, and disabled AI adapter behavior.
</verification>

<done>SEC-01 through SEC-04 and LOAD-01 are evidenced; no external provider is required for classroom safety.</done>
