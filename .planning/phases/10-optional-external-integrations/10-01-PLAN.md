---
phase: 10-optional-external-integrations
plan: 01
type: execute
wave: 1
depends_on: ["09-01"]
files_modified: ["lib/ai/*", "convex/internal/moderation.ts", "convex/http.ts", ".env.example", "tests/ai-adapter.test.ts", "docs/14_DOUBT_MODERATION_AI.md"]
requirements: ["MOD-02"]
user_setup: ["Select and configure an approved AI provider credential only when ready for live triage."]
---

<objective>Connect one approved AI moderation provider through the existing server-only adapter as the final optional integration, preserving exact disabled-provider behavior and classroom reliability.</objective>

<tasks>
1. Research the selected provider’s current official API, data policy, cost/rate limits, and SDK only at execution time. Record the selected provider and credential ownership in a short ADR; do not hardcode provider calls outside `lib/ai`.
2. Add server-only environment validation, minimal request construction, timeout/abort handling, strict response parsing, bounded retries, and redacted structured telemetry. Never send access tokens, student identities, or extra board content; never log doubt text or secrets.
3. Wire the internal moderation action to the adapter, retain deterministic prefiltering, and test success, disabled configuration, timeout, bad response, rate limit, and outage. Prove all failures reach the uncertain queue fallback rather than dropping a plausible doubt.
</tasks>

<verification>
- Provider credentials are server-only, documented in `.env.example` by variable name only, and absent from build output/log fixtures.
- Contract tests use fakes; any credentialed smoke is opt-in and records no user content.
- Disabling the provider leaves every classroom flow and P1 queue behavior usable.
</verification>

<done>MOD-02 is satisfied with a provider-agnostic, failure-safe live integration that can be disabled without product breakage.</done>
