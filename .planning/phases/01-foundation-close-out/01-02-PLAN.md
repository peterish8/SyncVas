---
phase: 01-foundation-close-out
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/protocol/socket.ts
  - tests/protocol.test.ts
  - socket-server/src/server.ts
  - components/foundation/socket-health-status.tsx
autonomous: true
requirements:
  - FOUND-03
user_setup: []
must_haves:
  truths:
    - "Shared protocol keeps SOCKET_PROTOCOL_VERSION = 1 with Zod .strict() envelopes"
    - "Board and viewport event names/payload schemas from docs/11 exist as validators + types"
    - "App and socket-server both import the shared protocol module successfully"
    - "Protocol rejects unknown fields and mismatched versions; no Yjs/cursor/chat events"
  artifacts:
    - path: "shared/protocol/socket.ts"
      provides: "SOCKET_EVENTS + Zod schemas for foundation, board, viewport, presence, protocol:error"
      exports: ["SOCKET_PROTOCOL_VERSION", "SOCKET_EVENTS", "boardUpdateSchema", "teacherViewportSchema"]
    - path: "tests/protocol.test.ts"
      provides: "Unit coverage for new validators"
      min_lines: 40
    - path: "socket-server/src/server.ts"
      provides: "Imports extended shared protocol (foundation handler remains)"
  key_links:
    - from: "components/foundation/socket-health-status.tsx"
      to: "shared/protocol/socket.ts"
      via: "@/shared/protocol/socket import"
      pattern: "SOCKET_EVENTS|foundationPongSchema"
    - from: "socket-server/src/server.ts"
      to: "shared/protocol/socket.ts"
      via: "relative ../../shared/protocol/socket.js import"
      pattern: "SOCKET_EVENTS|foundationPingSchema"
    - from: "docs/11_REALTIME_SOCKET_PROTOCOL.md"
      to: "shared/protocol/socket.ts"
      via: "event name strings + envelope fields"
      pattern: "board:update|teacher:viewport|board:current|protocol:error"
---

<objective>
Extend the shared Socket.IO protocol beyond foundation ping/pong with versioned, Zod-validated board/viewport/room/error schemas consumed by both the Next.js app and socket-server.

Purpose: FOUND-03 — give later board/follow/room phases a single typed contract without implementing full room handlers yet.
Output: Expanded `shared/protocol/socket.ts`, updated tests, and confirmed imports on app + socket-server.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-close-out/01-CONTEXT.md
@docs/11_REALTIME_SOCKET_PROTOCOL.md
@docs/28_API_AND_EVENT_ERROR_CODES.md
@shared/protocol/socket.ts
@tests/protocol.test.ts
@socket-server/src/server.ts
@components/foundation/socket-health-status.tsx

<interfaces>
<!-- Existing shared protocol — extend in place; keep foundation ping/pong working. -->

From shared/protocol/socket.ts:
```typescript
export const SOCKET_PROTOCOL_VERSION = 1 as const;
export const SOCKET_EVENTS = {
  foundationPing: "foundation:ping",
  foundationPong: "foundation:pong",
} as const;
// foundationPingSchema / foundationPongSchema use z.object({...}).strict()
// Types: FoundationPing, FoundationPong via z.infer
```

From socket-server/src/server.ts:
- Imports `SOCKET_EVENTS`, `SOCKET_PROTOCOL_VERSION`, `foundationPingSchema` from `../../shared/protocol/socket.js`
- Handles only `SOCKET_EVENTS.foundationPing` today — keep that behavior

From components/foundation/socket-health-status.tsx:
- Imports `SOCKET_EVENTS`, `SOCKET_PROTOCOL_VERSION`, `foundationPongSchema` from `@/shared/protocol/socket`

From docs/11 event names (must appear in SOCKET_EVENTS values):
- Client→server: `room:join`, `board:update`, `board:request-current`, `teacher:viewport`
- Server→client: `board:current`, `board:update`, `teacher:viewport`, `room:presence`, `protocol:error`
- Envelope base: `v: 1`, `sessionId: string`, `ts: number`, plus payload fields
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add versioned board/viewport/room protocol schemas</name>
  <files>shared/protocol/socket.ts, tests/protocol.test.ts</files>
  <read_first>shared/protocol/socket.ts, tests/protocol.test.ts, docs/11_REALTIME_SOCKET_PROTOCOL.md, docs/28_API_AND_EVENT_ERROR_CODES.md, .planning/phases/01-foundation-close-out/01-CONTEXT.md</read_first>
  <behavior>
    - Accept foundation ping/pong unchanged at SOCKET_PROTOCOL_VERSION = 1
    - Accept board:update / board:current / teacher:viewport / room:presence / protocol:error / board:request-current / room:join payloads that include v=1, sessionId, ts plus documented fields
    - Reject mismatched v, unknown keys (.strict()), and missing required fields
    - Reject / do not define student cursor, chat, or Yjs/CRDT event names
  </behavior>
  <action>
  Per D-04 keep `SOCKET_PROTOCOL_VERSION = 1` and Zod `.strict()` envelopes. Per D-05 extend `shared/protocol/socket.ts` beyond ping/pong; per D-06 do not add Yjs/CRDT types or student cursor events; do not add chat events.

  Update `SOCKET_EVENTS` to include (camelCase keys → exact string values):
  - roomJoin → "room:join"
  - boardUpdate → "board:update"
  - boardRequestCurrent → "board:request-current"
  - teacherViewport → "teacher:viewport"
  - boardCurrent → "board:current"
  - roomPresence → "room:presence"
  - protocolError → "protocol:error"
  Keep existing foundationPing / foundationPong entries.

  Add a reusable base envelope helper/schema fields: `v: z.literal(SOCKET_PROTOCOL_VERSION)`, `sessionId: z.string().min(1)`, `ts: z.number().int().nonnegative()` — used by classroom events (foundation ping/pong may stay as today without sessionId).

  Define Zod schemas + inferred types for at least:
  1. `roomJoinSchema` — envelope + optional noop/extra empty payload; enough to validate join intent (`sessionId` required).
  2. `boardUpdateSchema` — envelope + `boardVersion: z.number().int().nonnegative()` + scene representation field named `scene` typed as `z.unknown()` (or a structured object with `elements`/`appState` optional unknowns) + optional `files` metadata as `z.unknown().optional()`. Do NOT model raw pointer-move HF pen streams.
  3. `boardRequestCurrentSchema` — envelope only (request for canonical scene).
  4. `boardCurrentSchema` — envelope + `boardVersion` + `scene` (same scene typing as update).
  5. `teacherViewportSchema` — envelope + `x: z.number()`, `y: z.number()`, `zoom: z.number().positive()`, optional `pageId: z.string().optional()`.
  6. `roomPresenceSchema` — envelope + `connectedCount: z.number().int().nonnegative()` only (coarse count; no student identities/cursors).
  7. `protocolErrorSchema` — envelope fields as applicable + `code: z.string().min(1)` + `message: z.string().min(1)`. Prefer allowing codes from docs/28 (e.g. STUDENT_BOARD_EDIT_FORBIDDEN, STALE_BOARD_VERSION, INVALID_SOCKET_TOKEN) via `z.string()` or a z.enum of the documented socket-relevant codes — Claude discretion on enum vs string, but `code` must be machine-readable.

  Export schemas and `z.infer` types with clear names (e.g. `BoardUpdate`, `TeacherViewport`, `ProtocolError`).

  Expand `tests/protocol.test.ts` (TDD): add cases that (a) accept valid boardUpdate/teacherViewport/boardCurrent/roomPresence/protocolError fixtures, (b) reject wrong version and unknown keys, (c) assert SOCKET_EVENTS string values match docs/11 literals, (d) assert SOCKET_EVENTS has no keys/values containing cursor/chat/yjs. Keep existing foundation tests green.

  Package wiring discretion: keep file at `shared/protocol/socket.ts` (existing layout); do not create a separate publishable npm package unless required for typecheck — relative + `@/` imports already work.
  </action>
  <verify>
    <automated>.\node_modules\.bin\vitest.cmd run tests/protocol.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - shared/protocol/socket.ts exports `SOCKET_PROTOCOL_VERSION` equal to literal 1
    - shared/protocol/socket.ts SOCKET_EVENTS values include exact strings: foundation:ping, foundation:pong, room:join, board:update, board:request-current, teacher:viewport, board:current, room:presence, protocol:error
    - shared/protocol/socket.ts exports symbols named boardUpdateSchema, teacherViewportSchema, boardCurrentSchema, roomPresenceSchema, protocolErrorSchema (or equivalent exported const schemas with those roles — names above preferred)
    - rg over shared/protocol finds no event strings like student:cursor, chat:message, yjs, crdt
    - tests/protocol.test.ts covers accept + reject cases for at least board update and viewport schemas
    - `.\node_modules\.bin\vitest.cmd run tests/protocol.test.ts` exits 0
  </acceptance_criteria>
  <done>Versioned board/viewport/room/error validators exist with tests; forbidden event families absent.</done>
</task>

<task type="auto">
  <name>Task 2: Wire shared protocol imports into app and socket-server</name>
  <files>socket-server/src/server.ts, components/foundation/socket-health-status.tsx, shared/protocol/socket.ts</files>
  <read_first>socket-server/src/server.ts, components/foundation/socket-health-status.tsx, shared/protocol/socket.ts, socket-server/test/health.test.ts, socket-server/tsconfig.json</read_first>
  <action>
  Per D-05, types/validators must be imported by app and socket-server. Full room handlers wait for later phases — do not implement board broadcast/auth rooms here.

  1. Update `socket-server/src/server.ts` to import the expanded `SOCKET_EVENTS` object and at least two new schemas (recommended: `boardUpdateSchema`, `teacherViewportSchema`) from `../../shared/protocol/socket.js`. Keep the existing foundationPing handler behavior identical. Add a module-level reference that TypeScript cannot tree-shake away as unused in a way that breaks the import requirement — e.g. export a const `PROTOCOL_CONTRACT = { events: SOCKET_EVENTS, boardUpdateSchema, teacherViewportSchema } as const` from server.ts OR reference `SOCKET_EVENTS.boardUpdate` / `SOCKET_EVENTS.teacherViewport` inside a clearly commented `assertProtocolContract()` helper called once at module init that only checks typeof/string equality (no socket.on for classroom events yet).

  2. Confirm `components/foundation/socket-health-status.tsx` still imports from `@/shared/protocol/socket` and continues to use foundation ping/pong only. If needed, adjust imports so the app resolves the updated module (do not break the health UI). Optionally import `SOCKET_EVENTS.boardUpdate` into a non-UI constant in the same file only if required to prove the export surface — prefer leaving the component behavior unchanged.

  3. Do not add student cursor handlers, chat handlers, or Yjs. Do not persist anything to Convex from the socket server in this plan.

  4. Run socket-server typecheck and its health test to prove the shared import path still works under NodeNext resolution.
  </action>
  <verify>
    <automated>npm.cmd --prefix socket-server run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - socket-server/src/server.ts import list from shared/protocol includes SOCKET_EVENTS and at least one of boardUpdateSchema or teacherViewportSchema
    - socket-server/src/server.ts still registers socket.on for SOCKET_EVENTS.foundationPing only among classroom/foundation handlers (no board:update handler required yet)
    - components/foundation/socket-health-status.tsx still imports from `@/shared/protocol/socket`
    - `npm --prefix socket-server run typecheck` exits 0
    - `npm --prefix socket-server run test` exits 0 (foundation ping health test passes)
  </acceptance_criteria>
  <done>App and socket-server consume the extended shared protocol; foundation ping path remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → socket-server | Untrusted browser payloads cross into validation |
| Shared protocol module → app + socket-server | Single contract prevents divergent event shapes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Spoofing | board:update / teacher:viewport schemas | mitigate | Schemas exist now; server must still enforce role from signed token in later phases — do not trust a client `role` field (none added to payloads) |
| T-01-02-02 | Tampering | Envelope version `v` | mitigate | `z.literal(SOCKET_PROTOCOL_VERSION)` + `.strict()` rejects mismatched/unknown fields |
| T-01-02-03 | Information Disclosure | room:presence | mitigate | Only `connectedCount` — no student identities/cursors (D-06) |
| T-01-02-04 | Denial of Service | viewport event flood | accept | Throttle/coalesce is runtime behavior for Phase 4; schema only defines shape here |
| T-01-02-SC | Tampering | npm installs | accept | No new packages; zod already in root and socket-server dependencies |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] Protocol unit tests pass
- [ ] Socket-server typecheck + health test pass
- [ ] Event names match docs/11; no cursor/chat/Yjs events
- [ ] App health component still imports shared protocol
</verification>

<success_criteria>
- FOUND-03 satisfied: versioned validated board/viewport payloads usable by app and socket-server
- D-04, D-05, D-06 implemented exactly
- Foundation ping/pong remains operational
</success_criteria>

<output>
Create `.planning/phases/01-foundation-close-out/01-02-SUMMARY.md` when done
</output>
