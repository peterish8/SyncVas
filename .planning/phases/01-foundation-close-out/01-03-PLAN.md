---
phase: 01-foundation-close-out
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - package.json
  - app/page.tsx
  - components/foundation/convex-health-status.tsx
  - components/foundation/socket-health-status.tsx
  - convex/health.ts
  - socket-server/src/server.ts
  - socket-server/test/health.test.ts
  - tests/protocol.test.ts
autonomous: true
requirements:
  - FOUND-01
user_setup: []
must_haves:
  truths:
    - "npm run verify passes lint + typecheck + tests + build for app and socket-server"
    - "Convex health query api.health.status still returns { status: 'ready' }"
    - "Socket-server foundation ping/pong health path still responds"
    - "Next.js foundation page still renders Convex and Socket health status components"
  artifacts:
    - path: "package.json"
      provides: "verify/lint/typecheck/test:all/build scripts"
      contains: "\"verify\""
    - path: "convex/health.ts"
      provides: "Public health query"
      exports: ["status"]
    - path: "socket-server/test/health.test.ts"
      provides: "Automated socket ping/pong smoke"
  key_links:
    - from: "components/foundation/convex-health-status.tsx"
      to: "convex/health.ts"
      via: "useQuery(api.health.status)"
      pattern: "api\\.health\\.status"
    - from: "components/foundation/socket-health-status.tsx"
      to: "socket-server/src/server.ts"
      via: "SOCKET_EVENTS.foundationPing / foundationPong"
      pattern: "foundationPing|foundationPong"
    - from: "package.json"
      to: "socket-server/package.json"
      via: "npm --prefix socket-server in typecheck/test:all"
      pattern: "test:all|typecheck"
---

<objective>
Make the brownfield monorepo health smoke path reliable and ensure `npm run verify` is green after schema + protocol close-out.

Purpose: FOUND-01 — developers can trust lint/typecheck/test/build and see Next.js, Convex, and socket-server health respond.
Output: Passing verify pipeline; confirmed Convex + socket health wiring; no broken foundation UI imports.
</objective>

<execution_context>
This plan is self-contained. Follow the referenced repository docs and do not rely on an external agent-specific workflow path.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-close-out/01-CONTEXT.md
@.planning/phases/01-foundation-close-out/01-01-SUMMARY.md
@.planning/phases/01-foundation-close-out/01-02-SUMMARY.md
@package.json
@convex/health.ts
@components/foundation/convex-health-status.tsx
@components/foundation/socket-health-status.tsx
@app/page.tsx
@socket-server/test/health.test.ts

<interfaces>
From package.json scripts (must remain or improve, not delete):
- lint: eslint app components convex shared tests socket-server
- test / test:all: vitest root + socket-server
- typecheck: tsc --noEmit && npm --prefix socket-server run typecheck
- build: next build
- verify: npm run lint && npm run typecheck && npm run test:all && npm run build
- dev:all / dev:socket: local smoke runners

From convex/health.ts:
- `export const status = query({ args: {}, handler: async () => ({ status: "ready" as const }) })`

From components:
- ConvexHealthStatus uses `useQuery(api.health.status)` when NEXT_PUBLIC_CONVEX_URL configured
- SocketHealthStatus emits foundationPing to NEXT_PUBLIC_SOCKET_URL (default http://localhost:4001)

Brownfield rule: do not re-scaffold Next.js/Convex/socket-server from zero. Fix wiring only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair verify scripts and clear regressions from schema/protocol</name>
  <files>package.json, app/page.tsx, components/foundation/convex-health-status.tsx, components/foundation/socket-health-status.tsx, convex/health.ts, tests/protocol.test.ts, socket-server/src/server.ts, socket-server/test/health.test.ts</files>
  <read_first>package.json, socket-server/package.json, convex/health.ts, components/foundation/convex-health-status.tsx, components/foundation/socket-health-status.tsx, app/page.tsx, .planning/phases/01-foundation-close-out/01-01-SUMMARY.md, .planning/phases/01-foundation-close-out/01-02-SUMMARY.md</read_first>
  <action>
  Per D-07, `npm run verify` (existing root script) must pass lint + typecheck + tests + build for app and socket-server. This plan owns making that true after plans 01-01 and 01-02.

  1. Read package.json scripts and keep the verify chain covering: lint, typecheck (root + socket-server), test:all (root + socket-server), build. If any script is missing or incorrectly scoped after prior work, fix the script strings — do not replace the monorepo with a new toolchain.

  2. Run the verify pipeline using the local toolchain available on Windows (prefer `npm.cmd run verify` via the working Node install, or equivalent `.\node_modules\.bin\` tools if npm shim is broken). Fix any TypeScript/ESLint/test failures caused by schema or protocol changes. Allowed touch set is listed in files_modified; prefer minimal fixes.

  3. Preserve `convex/health.ts` public `status` query returning `{ status: "ready" }`. Preserve foundation page composition in `app/page.tsx` that renders `ConvexHealthStatus` and `SocketHealthStatus`.

  4. Do not add Yjs, student cursors, chat, Excalidraw, or room lifecycle features here. Do not weaken tests to pass.
  </action>
  <verify>
    <automated>npm.cmd run verify</automated>
  </verify>
  <acceptance_criteria>
    - package.json still defines script `verify` that invokes lint, typecheck, test:all, and build (by script composition or inline)
    - package.json `typecheck` still typechecks socket-server (contains `npm --prefix socket-server run typecheck` or equivalent)
    - package.json `test:all` still runs socket-server tests
    - `npm.cmd run verify` exits 0
    - convex/health.ts still exports `status` query returning status ready
    - app/page.tsx still references ConvexHealthStatus and SocketHealthStatus
  </acceptance_criteria>
  <done>Monorepo verify pipeline is green with foundation health modules intact.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm local Convex + socket health smoke path</name>
  <files>convex/health.ts, socket-server/test/health.test.ts, components/foundation/socket-health-status.tsx, components/foundation/convex-health-status.tsx</files>
  <read_first>convex/health.ts, socket-server/test/health.test.ts, socket-server/src/server.ts, components/foundation/convex-health-status.tsx, components/foundation/socket-health-status.tsx, .env.local, .env.example</read_first>
  <action>
  Per D-08, existing Convex health query + socket foundation ping/pong remain the local smoke path; fix wiring if broken.

  Execute and record evidence for all three FOUND-01 responders:

  1. Socket-server: run `npm.cmd --prefix socket-server run test` and confirm health.test.ts answers a valid versioned foundation ping with foundationPong. If the test fails due to protocol import breakage, fix imports/handlers without removing the ping path.

  2. Convex: with linked deployment from .env.local (`CONVEX_DEPLOYMENT` / `NEXT_PUBLIC_CONVEX_URL`), run `npx convex run health:status` (or `npx convex run health:status --value {}` if required by CLI). Expect JSON/object including status ready. If the deployment is not linked, attempt `npx convex codegen` already done in 01-01 and document the exact blocker — but still keep the query source correct. Do not delete the health query.

  3. Next.js app: ensure `npm.cmd run build` already passed in Task 1 (proves the app compiles with health components). Optionally start is not required if build + components smoke compile; do not re-scaffold the app.

  4. If smoke wiring is broken (wrong event names, broken api.health import, wrong SOCKET URL default), fix only the health path files listed. Default socket URL remains `http://localhost:4001` when NEXT_PUBLIC_SOCKET_URL unset.

  5. Add no new external services. No production deploy.
  </action>
  <verify>
    <automated>npm.cmd --prefix socket-server run test</automated>
  </verify>
  <acceptance_criteria>
    - socket-server/test/health.test.ts still emits SOCKET_EVENTS.foundationPing and asserts foundationPongSchema
    - `npm.cmd --prefix socket-server run test` exits 0
    - `rg -n "api\\.health\\.status|status:\\s*\\\"ready\\\"|status: \\\"ready\\\"" components/foundation/convex-health-status.tsx convex/health.ts` shows health wiring present
    - `rg -n "foundationPing|NEXT_PUBLIC_SOCKET_URL|localhost:4001" components/foundation/socket-health-status.tsx` matches
    - When Convex deployment is linked, `npx convex run health:status` returns a result whose status field is ready; if unlinked, SUMMARY.md must state the exact CLI error and that source query remains correct (do not claim live Convex smoke passed)
  </acceptance_criteria>
  <done>FOUND-01 smoke path verified for socket (automated) and Convex (CLI or explicitly blocked), with Next.js health UI still wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local dev machine → Convex deployment | Health query uses configured deployment URL/env |
| Browser → socket-server | Foundation ping is unauthenticated health-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Information Disclosure | health endpoints | accept | Health returns non-sensitive ready status only; no secrets in response |
| T-01-03-02 | Spoofing | foundation ping without auth | accept | Intentional unauthenticated liveness for local smoke; classroom events remain ungated until Phase 3 token auth |
| T-01-03-03 | Information Disclosure | NEXT_PUBLIC_* env in browser | mitigate | Only public Convex/socket URLs in client bundle; SOCKET_INTERNAL_SECRET must stay server-side (do not add to client components) |
| T-01-03-SC | Tampering | npm installs during verify fixes | mitigate | Do not add new dependencies to fix verify; adjust code/scripts only. If a dependency is unavoidable, stop and request legitimacy review before install |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] `npm.cmd run verify` exits 0
- [ ] Socket health test passes
- [ ] Convex health query source intact; live CLI smoke run when linked
- [ ] Foundation page still mounts both health components
</verification>

<success_criteria>
- FOUND-01 satisfied: Next.js, Convex health, and socket-server ping path are green under verify/smoke
- D-07 and D-08 implemented
- Phase 1 exit: developer can trust local health + verify scripts on the brownfield scaffold
</success_criteria>

<output>
Create `.planning/phases/01-foundation-close-out/01-03-SUMMARY.md` when done
</output>
