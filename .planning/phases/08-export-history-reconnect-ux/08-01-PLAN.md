---
phase: 08-export-history-reconnect-ux
plan: 01
type: execute
wave: 1
depends_on: ["07-01"]
files_modified: ["convex/exports.ts", "convex/sessions.ts", "convex/internal/*", "app/teacher/history/page.tsx", "components/export/*", "components/connection/*", "tests/export-history-reconnect.test.ts"]
requirements: ["EXPORT-02", "EXPORT-03", "EXPORT-04", "UX-01", "UX-02"]
---

<objective>Make a completed class useful and recoverable: queued export with visible state, teacher history, safe reconnect/resync, and humane error/empty states.</objective>

<tasks>
1. Implement owner-checked export job records and internal generation actions. Persist job state, expose minimal reactive status, use durable final scene input, and prevent optional summary failures from affecting scene persistence. Do not select an external PDF/image service yet; use an in-process/documented implementation when feasible and leave any provider adapter optional.
2. Build accessible history and export-status UI plus connection chips that appear only when degraded. On reconnect, obtain/refresh token, rejoin room, request `board:current`, reconcile version, and resume viewport; never replay stale local mutation as teacher.
3. Test export authorization/status transitions, empty history, failure isolation, teacher/student reconnect, version-gap resync, and clear next-step messages for each failure state.
</tasks>

<verification>
- Teacher sees queued/processing/ready/failed export state and past class metadata.
- Reconnect restores the latest scene without changing a student’s local follow/free-roam choice unless the user explicitly returns to follow.
- All request actions show loading/disabled feedback and human-readable failure recovery.
</verification>

<done>EXPORT-02 through EXPORT-04 and UX-01 through UX-02 are complete without a required third-party integration.</done>
