---
phase: 02-board-proof
plan: 01
type: execute
wave: 1
depends_on: ["01-01", "01-02", "01-03"]
files_modified: ["app/teacher/page.tsx", "app/student/[sessionId]/page.tsx", "components/board/*", "shared/protocol/socket.ts", "socket-server/src/server.ts", "tests/board-sync.test.ts"]
requirements: ["BOARD-01", "BOARD-02", "BOARD-03", "BOARD-04", "BOARD-05", "BOARD-06"]
---

<objective>Deliver the smallest real Excalidraw board proof: a teacher is the only writer, two read-only student views receive board state over Socket.IO, and student navigation remains local.</objective>

<tasks>
1. Install and embed the MIT Excalidraw package only after confirming its current license/version; build a desktop teacher route with immediate local scene updates, stylus-safe board space, and the documented neutral canvas chrome.
2. Add authenticated-room-independent proof handlers for `board:update` and `board:request-current`: validate the shared versioned envelopes, retain only the latest canonical scene/version in socket-server memory, reject non-teacher update attempts, broadcast current/newer state, and never persist pointer events to Convex.
3. Build the read-only student board client: request the current scene on connect, apply only newer versions, request resync on a version gap, and block Excalidraw editing tools. Keep student pan/zoom in local component state; add unit/integration tests for teacher-only mutation, late join, stale versions, and isolated viewports.
</tasks>

<verification>
- `npm.cmd run verify` passes.
- Two browser sessions demonstrate teacher stroke visibility; a late student sees the current scene without replay.
- Socket tests prove student `board:update` is rejected and one student pan/zoom does not emit a teacher viewport or mutate another client.
- Manual XP-Pen verification is recorded as pending until Phase 9 production-device validation.
</verification>

<done>BOARD-01 through BOARD-06 pass in a deliberately minimal proof room with no student editor, cursors, chat, CRDT, or Convex pen stream.</done>
