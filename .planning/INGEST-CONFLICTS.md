# Ingest Conflicts

### BLOCKERS (0)

None.

### WARNINGS (0)

None.

### INFO (3)

1. **Scaffold already present** — `docs/22` Milestone 0 partially implemented in repo (Next.js, Convex, socket-server). Roadmap should start brownfield, not re-scaffold from zero.
2. **AI summary pipeline** (`docs/15`) is product-adjacent but MVP marks AI structured notes as P2 / Milestone 8 — classified as **v2 deferred**, not v1 phase work. End-class must still succeed if AI summary fails.
3. **P0 vs P1 gate** — `docs/03` says do not start P1 until all P0 acceptance tests pass in 2 student tabs + 1 teacher tab. Encode as phase dependency: doubts moderation/export polish after core board+follow+basic doubts.
