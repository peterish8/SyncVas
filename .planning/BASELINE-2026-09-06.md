# Baseline verification — 2026-09-06

Captured before the production-readiness remediation changes. The working tree was already dirty; no existing changes were reset or discarded.

| Check | Result | Evidence |
|---|---|---|
| `npm.cmd run test:all` | Pass | Root: 9 files passed, 57 tests passed, 4 skipped. Socket: 2 files passed, 3 tests passed. |
| `npm.cmd run lint` | Pass | Exit code 0; no diagnostics. |
| `npm.cmd run typecheck` | Pass | Root `tsc --noEmit` and socket-server typecheck both completed with exit code 0. |
| `npm.cmd run build` | Blocked | Next.js 16.3.4 reported `Another next build process is already running` because the existing `.next/dev/lock` belongs to an active Next dev server. The active server was left untouched. |

The build result is a baseline environment limitation, not a passing build claim. It must be rerun after the active development server is stopped by its owner.
